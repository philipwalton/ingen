var site = require('../lib/site')

site.on('beforeBuild', function() {

  debugger

  // TODO: add the ability to create pages without a file
  // new Page({
  //   title: taxonomyName,
  //   content: '',
  //   permalink: taxonomyName + '.html'
  // })

  // create a new Page instance that has the taxonomy data

})
